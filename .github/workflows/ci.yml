name: Run Tests on Ci
on:
  push:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Install Dependencies
        run: yarn
      - name: Run Tests
        run: yarn ci-test